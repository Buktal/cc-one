# 更新履歴

cc one の主な変更点をすべて記録します。

形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に基づき、本プロジェクトは[セマンティックバージョニング](https://semver.org/lang/ja/)に従います。

## [Unreleased]

### 変更

- **VaultOne から cc one に改名** —— アプリ・リポジトリ・パッケージは `cc-one`、中国語名は「归一（きいち）」に統一。旧構成ディレクトリは初回起動時に自動移行されます。

### 修正

- **840×600 の最小ウィンドウが実際に効くように** —— 軽量モード復元時に古い 720×520 の下限が `set_min_size` で再適用され、引き上げた最小サイズを静かに上書きしていました。グランスカード往復後のダッシュボードが欠け小さいサイズで復元されなくなります。
- **狭いウィンドウでセッション表が重ならなくなった** —— 固定 7 列がタイトル列を 0 まで圧縮し、ヘッダー文字がプロジェクト列に溢れていました。表は読みやすい幅を保ち、あふれた分は横スクロールします。

## [1.7.1] - 2026-08-06

### 追加

- **起動時にアップデートを確認** —— 起動のたびに GitHub Releases を確認し、起動中は 6 時間ごとに再確認します。新しいバージョンがあるとフッターに表示されます。

- **グループのドラッグ並べ替え** —— セッションサイドバーの任意のカスタムグループを掴んで新しい位置へドラッグできます。並べ替えた順序はそのまま維持され、お気に入りタブでは他のすべてのデバイスにも同期されます。順序はトラックごとに保存され（ローカルグループはデータベースの position 列、同期グループは同期ファイルの position フィールド）、新規グループは常に末尾に追加されます。「すべて / 未グループ」の行は固定されたままです。

### 変更

- **余裕のある最小ウィンドウ** —— メインウィンドウの最小サイズを 840×600 に引き上げ、セッション一覧がグループサイドバーに重ならないようにしました。狭いウィンドウではセッションカードのタイトルが省略記号で切り詰められます。

- **スッキリしたグループサイドバー** —— グループ行からフォルダアイコンを削除し、サイドバーを細くしました。セッションテーブルに余裕が生まれます。

- **セッション詳細パネルの磨き込み** —— タイトルのリネーム発動領域を「タイトル＋鉛筆アイコン」のみに縮小し（空白クリックで編集が始まらないように）、ソースをタグチップとして表示し、お気に入りボタンの高さをグループ選択と揃え、吹き出しは狭いウィンドウで 80% に上限（広いウィンドウでは従来どおり 72ch）。

## [1.7.0] - 2026-08-06

### 追加

- **すべてのメッセージにコピーボタン** —— セッションのトランスクリプトでメッセージ行にホバー（またはキーボードフォーカス）するとコピーボタンが現れ、クリックで原文をクリップボードにコピーし、一瞬チェックマークを表示します。
- **ウィンドウに追従する詳細パネル** —— セッション詳細パネルはウィンドウの約 7 割（`100vw - 32rem`）を占め、背後にサイドバーと一覧のタイトル列が見えたままになるため、どのセッションを開いているか常に分かります。

### 変更

- **3 声部のトランスクリプトレイアウト** —— assistant メッセージは左、user メッセージは右（ミラーした吹き出し、角は外側に向けて切る）、tool と system 行は中央に全幅で並ぶ「ワークベンチ」。位置だけで誰の発言かが分かります。
- **より締まったメッセージヘッダー** —— アイコン・時刻・モデルバッジが 1 行に並びます。user メッセージではグループが吹き出しの右端にミラーされ、時刻が右端に揃います。
- **折りたたみ可能なメッセージ、静かな tool 行** —— どのメッセージもクリックで折りたたみ（デフォルトは展開）。tool 行はデフォルトでツール名 1 行に折りたたまれ、出力は等幅フォントのコードパネルとして表示されます。
- **グループ件数の整列** —— グループサイドバーの件数は常に右端に揃い、「すべて / 未グループ」の通常行と一致。編集メニューはホバー時に件数をスライドさせて現れ、通常時はスペースを占めません。

### 修正

- **tool 行の整理** —— 重複していたアイコン 2 つを削除。名前のないツールは内容の先頭行を切り詰め、行を引き伸ばしません。

## [1.6.0] - 2026-08-06

### 追加

- **セッションブラウザ** —— サイドナビゲーションに新エントリが加わり、AI CLI が書き出す生のセッションログをブラウズ・検索可能な履歴にします。各セッションはプロジェクトディレクトリの下に並び、完全なトランスクリプト・セッションごとのトークン内訳・コスト（使用量レコードからのライブ集計で、二重保存は一切なし）を表示。期間 / ソース / モデル / デバイスでフィルタし、タイトルとパスで検索。その場でのリネームにも対応し、どのセッションも詳細パネルで開けます（ロール別に色分けされたトランスクリプト：assistant はモデルバッジ付き、user は別トーン、tool 行は折りたたみ可能）。
- **2 タブ、2 通りの整理方法** —— **ローカル** タブはこの端末で収集した全セッションを、決して端末の外に出ないプライベートグループに整理して表示。**お気に入り** タブは全デバイスでお気に入り登録したセッションを、すべての端末で共有される同期グループに整理して表示し、各エントリにソースデバイスが付記されます。同じセッションはタブごとに別のグループに属せます。
- **お気に入りの端末間同期** —— スターを付けると、そのセッションのトランスクリプトと同期グループへの配置が同期リポジトリ経由で全デバイスに公開されます。スターを外すとどこからでも消えます。端末の外に出るのはお気に入り登録したセッションだけ——それ以外はすべてローカルに留まります。
- **すべてのセッションのトランスクリプトが即座に閲覧可能** —— 会話本文は収集時にすべてローカルデータベースへ書き込まれるため、お気に入り登録の有無に関係なくどのセッションもすぐ完全なトランスクリプトを開けます。書き込み中のログファイルを再読取することはありません。
- **より速い収集** —— スケジューラのプリセットに 5 秒間隔が加わり、ダッシュボードがほぼリアルタイムになります。

### 変更

- **同期をダーティデイ追跡で再構築** —— collect はローカルストアだけに書き込み、同じトランザクション内で影響を受けた日をダーティとして記録。push はストアから当日分のアーティファクトを決定論的に再生成し（バイト安定、append ではない）、push が確定してからダーティを解除します。従来の JSONL-first 書き込み順、with-own-data のスナップショット/復元保護、アーティファクト欠落の reconcile はすべて撤去——書き込み経路は 1 本だけになり、2 台の端末が同一ファイルの内容について食い違うことはあり得ません。
- **セッションスナップショットは書き込みではなく派生に** —— お気に入りセッションの同期スナップショットは push 時にストアから再計算されます。ソースログが消えたセッションは全デバイスで自動回収されます（ローカル行と同期スナップショットの両方を削除）。
- **同期の対象範囲を縮小** —— 任意の同期リポジトリが運ぶのは、使用量・お気に入りセッション・ライブラリファイルのみになりました。1.5.x の Sync-config（リポジトリ経由でアプリ設定を同期する機能）は削除され、設定は再び端末ごとに閉じています。
- **より賢いプロジェクト分類** —— セッションのプロジェクトディレクトリは、そのセッションのイベントで最も多く出現する作業ディレクトリ（最初の 1 件ではなく最頻値）から導出されます。サブディレクトリで始まったセッションも本当のプロジェクトルートに分類されます。

### 修正

- **セッション収集の堅牢化** —— 文字の途中で終わるログファイル（書き込み中のセッション、例：中国語）は丸ごと破棄せず lossy 読み取りに切り替え、進行中のセッションがファイルごと失われることがなくなりました。スキャンカーソルは正常に進み、リネームに追随してタイトルが更新され、欠落したセッションは補完されます。
- **お気に入り未登録のセッションも閲覧可能に** —— 従来は開こうとするとお気に入り登録を求められました（トランスクリプトがお気に入りスナップショットの中にしかなかったため）。トランスクリプトはローカルデータベースを読むようになり、お気に入り登録の有無に関係なくどのセッションも開けます。
- **お気に入り状態のちらつきを解消** —— 再フェッチが古いスナップショットにフォールバックしなくなり、お気に入り登録直後のセッションは登録状態を維持。スターアイコンもテーブル行と一致します。
- **セッションのレイアウト** —— 「新規グループ」ボタンが常に表示され、グループサイドバーが領域からはみ出さなくなりました。

### 内部

- **これまで最大のアーキテクチャ整理** —— db の god-module をドメイン別 module（`store_*`）に分割。22 個の command をドメイン module から `commands.rs` へ移行し、モデル正規化・価格マッチング・デバイスレジストリ・provider 解析・UI フォーマット・日付範囲チップなど、単一情報源への収斂を全面的に実施。sync の god-module は git プリミティブ + flow オーケストレーションの 2 層に分割し、レビューによる整理で残っていた乖離を解消しました。ユーザーに見える変化はありません——ダッシュボードのコードはより拡張しやすくなっています。

## [1.5.1] - 2026-07-31

### 修正

- **ローカルコミットがリモートのパッチと重複する場合も同期が自己修復** —— 1.5.0 の rebase 自己修復は、ローカルコミットがすでにリモートにあるパッチと重複する場合（例：2 台の端末がそれぞれ同じデバイス整理を実行）に中断し、`rebase onto remote tip would conflict ... this patch has already been applied` と表示されて端末が再び分岐状態のまま固まることがありました。`pull` は rebase 時に適用済みのコミットを破棄して続行するようになり、分岐の自己修復が止まらなくなります。
- **使用量行が同期から静かに欠落しなくなる** —— ingest は従来 SQLite を先に書き、JSONL アーティファクトは単なるバックアップ扱いで追記エラーを飲み込んでいました。DB には入ったがアーティファクトに漏れた行（一時的な追記失敗、または ≤1.3.x の残骸）は以後永久にロックされ——台帳の重複除外が以降の collect をすべて静かに無視し、アーティファクトを pull したピアには永遠に現れませんでした（同じフィルタで一方の端末が約 24M token、ピアが約 30M と表示される例も）。ingest は JSONL アーティファクトへの追記を先に冪等に行い、追記エラーを伝播します。追記に失敗するとスキャンカーソルは動かず、次回の collect で同じソース行を再解析します。新設された collect 前の reconcile は、ストアにありアーティファクトにない行があるとカーソルをクリアし、1 回の再スキャンで既存の欠落をバックフィルします——端末は手動修復なしで収束します。

## [1.5.0] - 2026-07-31

### 追加

- **Grok CLI** —— Grok Build のセッションログからトークン使用量を読み取る、5 つ目の対応 AI CLI（本バージョンのリリースノートで記載漏れとなっていたため、遡って追記）。

### 変更

- **設定のレイアウト** —— 独立した Cloud-config セクションが Sync に統合されました。"Sync config" ボタンと競合リゾルバは "Sync now" と並び、1 枚の Sync カードにまとまります。セクション順は General / This machine / Devices / Sync / Maintenance になり、"Sync cloud config" ボタンは "Sync config" と表示されます。

### 修正

- **分岐した push の後の同期自己修復** —— デバイスが push 競争に負けた場合（別のピアが pull と push の間に先に push）、以後すべての "Sync now" / "Sync config" が `pull would diverge on 'main'; refusing to auto-merge` で失敗し、自分では回復できずダッシュボードは古いデータのままでした。`pull` はデバイス固有のローカルコミットをリモートの先端に rebase してから push し、分岐を自動修復します。デバイス分離（`data/<deviceId>/`）により rebase は無衝突で、両端末のデータはリモートに並存します——ソフトリセットのみの修復ではローカルツリーをそのまま再生してピアのデータを破壊していたところでした。
- **過去 1 日を選んだときのトレンドチャート** —— 過去の 1 日（例：2026-07-30 → 2026-07-30）を選ぶと使用量トレンドが平らなゼロ線に潰れていました：チャートは選択した日ではなく *今日* の時間帯をゼロ埋めしていたため、実際のレコードが一致しなかったのです。現在は選択した日の 24 時間軸（00:00 → 23:00、当日は現在時刻まで）を埋めるようになりました。

## [1.4.0] - 2026-07-30

### 追加

- **Library —— 端末別ファイル中継** —— ファイル / ディレクトリをウィンドウにドラッグするとアップロード（＝同期リポジトリの該当端末サブツリーへの push）。ディレクトリを段階的に掘り下げ（アップロード・エクスポート・単一ファイルダウンロードは任意の深さで動作）、アプリ内でファイルをプレビュー（画像は幅にフィット、ctrl+ホイールでズーム、それ以外はサンドボックス iframe）、任意のパスへエクスポート。アップロードが唯一の自動方向で、エクスポートは手動のまま、AI ツール自身の設定ディレクトリには一切書き込みません。同名同種は上書き（git 履歴が安全網）、同名異種は拒否。ピアの忘却ではそのファイルを `from-<peer>/` 配下へ移行するか削除するかを選べます。

### 変更

- **ピッカーのラベル** —— ログとダッシュボードのデバイス / ソース / モデルのドロップダウンが薄い `.` 付きプレースホルダを廃止。「すべて」は正式ラベル（すべてのデバイス / すべてのソース / すべてのモデル）を表示し、日付範囲チップは同日内の範囲を折りたたみ、ダッシュボードの狭い列で折り返さなくなりました。

### 修正

- **使用量フィルタ** —— ダッシュボードフィルタが具体日付ではなく期間プリセット（today / 7d / 30d / all / custom）を保持するようになり、「今日」が午前0時以降に「昨日」として読み戻されることがなくなりました。プリセットの無い従来レコードは "custom" にフォールバックしリテラル日付を維持します。
- **同期の重複除外** —— `usage_records` / `turn_durations` / `ledger` が `uuid` をグローバル主キーとしていたため、同一ソースイベントが2つのデバイス id で再再生されると1行に畳まれ、あるデバイスのデータが別デバイスに帰属する恐れがありました。重複除外を `(uuid, device_id)` で行うよう変更（既存の単列主キーを複合キーへ移行、行数は保持）。同期リポジトリのバインド直後に pull し、ピアデバイスが再起動なしで出現するように。
- **ウィンドウ最小サイズ** —— 変形するメインウィンドウがグランスカードの小さなサイズで復元・スナップショットされることがありました。完全ダッシュボードは 720×520 の最小サイズを強制（ライトウェイト dock はクリア、完全復元で再適用）し、古い最小サイズ未満の rect は次回復元時に自己修復します。

## [1.3.1] - 2026-07-28

### 変更

- **ソースフィルタの可視性** —— ソースデータが1つでも存在すればソースドロップダウンをレンダリングし、単一ソースのユーザーにもフィルタが見えるように（以前は収集ソース ≥2 が必要でした）。
- **フィルタチップのサイズ** —— ログコントロールバーのフィルタチップを典型的な内容でサイズ調整（モデル `w-48`、ソース `w-40`、デバイス `w-36`）。ダッシュボードのカード列は引き続き均一幅 `w-36`。

### 修正

- **リリースの完全性** —— `v1.3.0` が2コミット早くタグ付けされており、上記のソースフィルタ・チップサイズ変更が 1.3.0 インストーラに含まれていませんでした。`v1.3.1` は現在の `main` にタグ付けしてそれらを取り込みます。

## [1.3.0] - 2026-07-28

### 追加

- **マルチ CLI の使用量収集** —— 使用量収集が、ローカルにログを書く 4 つの AI CLI に対応しました：Claude Code（`~/.claude`）、Codex（`~/.codex`）、Gemini CLI（`~/.gemini`）、OpenCode（SQLite）。各ソースのトークン口径は同じ 4 バケットモデルに正規化されます——Codex のキャッシュ込み input は fresh input に変換、Gemini の `thoughts` は output に折込、OpenCode の `cache.write` は cache creation に対応付け。Claude Code の重複除外は最適な message-id スナップショット（`stop_reason` があるもの、なければ最大 output）を選ぶようになり、message_start スナップショットが固定されて output を過小計上することはなくなりました。gpt-5.5 / 5.4 / 5.2（プレフィックスフォールバックで `-codex` 系をカバー）と DeepSeek v3.x のシード価格を追加。
- **デバイススコープの使用量追跡** —— ダッシュボード、展開済みライトウェイトカード、格納ミニバーの 3 ウィンドウすべてをデバイス単位で絞り込めます。統一されたデバイスピッカーが 3 ウィンドウを駆動。格納バーのホバードロワーはホバーで開き、クリックでデバイス一覧を表示。
- **デバイスライフサイクル** —— ピアデバイスをローカルから忘却（行とアーティファクトディレクトリを削除）。git 上に存在しなくなった古いピアは、同期 pull と収集パスの両方で約 30 秒以内に自動消去され、まだアクティブなピアは次回同期で自己修復します。最近のリクエストとログのデバイス列にデバイス名が表示されるように。
- **使用量フィルタの永続化** —— 期間 / モデル / デバイススコープのフィルタがアプリ再起動後も保持されます。
- **形状別ウィンドウ座標の記憶** —— full / expanded / tucked の各形状が自身の配置と状態を記憶し、形状切替時にリセットされず前回位置が復元されます。
- **システムプロキシ追従の同期** —— libgit2 の push/fetch/clone/connect が OS のシステムプロキシ（環境変数、次に Windows レジストリ）に追従するようになり、Clash/Mihomo や企業ゲートウェイ配下の同期クライアントが黙ってタイムアウトすることはなくなりました。プロキシ変更は次回同期で即時反映、キャッシュしません。

### 変更

- **UI の洗練** —— タイトルバーを 36px に引き上げ、画面中央のデバイスセレクタを引き締めました。トークン列のヘッダには言語中立な `tok` 単位を採用。セレクトポップオーバーは内容に自動フィット（トリガより狭くならない）し、既定で上端揃えで開き、モデルドロップダウンの飛びを修正しました。

### 修正

- **Linux トレイ** —— 「Show」エントリを追加し、トレイからウィンドウを復元できるように。libappindicator/SNI バックエンドは Linux でトレイクリックイベントを一切発火しないため、左クリックが無効でした。Windows/macOS の左クリック復元は変更ありません。
- **シード価格** —— 不正なシードリテラルは起動時に panic するようになり、黙って 0 を返してすべてのコスト計算を歪めることがなくなりました。
- **本日のトレンド** —— 「本日」のトレンドが現在時刻で止まらず、1 日全体をカバーするように。
- **リクエストログ** —— フィルタ切替時にログページをリセットし、古く範囲外のページに迷い込まないように。

## [1.2.0] - 2026-07-24

### 追加

- **ライトウェイトグランスモード** —— メインウィンドウが画面右端にドッキングした小さな常時最前面の「今日」スナップショットに変形します。互いに切り替え可能な 2 つの形状：今日のトークン合計を常時表示する格納ミニバーと、ダッシュボードのアンカーを再利用した展開カード。full ⇄ expanded ⇄ tucked をどの形からも切り替え。
- **マルチスキンテーマ** —— 5 つのアクセント＋チャート配色（Neutral / Sage / Azure / Crimson / Mauve）でアプリ全体をリカラー。既定は Neutral（グレースケール）。デバイスごと、同期対象外。

### 変更

- **使用量トレンド** —— トレンドチャートが単線からデータポイント付きマルチラインになり、各指標が独自に読み取れるように。

### 修正

- **ライトウェイトモード** —— 格納バー全体がドラッグ可能に（小さな隅のグリップだけではなく）。押下で「クリックで展開」と「ドラッグで移動」を引き続き区別。

## [1.1.0] - 2026-07-23

### 追加

- **自動更新** —— 起動時に新バージョンを確認（24 時間に 1 回までスロットル）、または設定から手動確認。署名付きインストーラーを GitHub Releases から直接ダウンロード・インストールし、Ed25519 署名検証とワンクリック再起動に対応。GitHub 経由で完全に配信——セルフホストサーバー不要。更新失敗時は Releases ページを開く手動フォールバック。
- **表示言語** —— UI を English / 简体中文 / 日本語 で切り替え。

### 修正

- **ライトウェイトモード** —— 格納された覗き見アイコンのエッジ吸着と、斜め方向の展開アニメーションを滑らかに。

## [1.0.0] - 2026-07-23

初の公開リリース（オープンソース）。

### 追加

- **ダッシュボード** —— 4 バケットのトークン消費（input / output / cache creation / cache read）、キャッシュヒット率（`cache_read / (input + cache_creation + cache_read)`）、総リクエスト数と総コスト（USD、収集時に固定）、トークン対コストの双軸使用量トレンド、コールごとのリクエストログ（モデル・トークン内訳・コスト・ターン所要時間・`stop_reason` / `service_tier` チップ）、ターンごとのコストと実所要時間ビュー。
- **収集** —— Claude Code セッションログの読み取り専用解析（ソースログは変更しない）、カーソルベースの増分スキャン、トレイ常駐のバックグラウンドスケジューラ。プラグイン可能なプロバイダアーキテクチャ（現在は Claude Code、追加予定）。
- **同期（任意）** —— スタンドアローンモード（完全なダッシュボード、ネットワークゼロ）と同期モード（自分が管理する GitHub リポジトリ経由で端末間の使用量を整合）。プレーンテキストのアーティファクトを端末と日付で分割（`data/<device>/usage-YYYY-MM-DD.jsonl`）。
- **コストと価格** —— 編集可能なモデルごとの価格上書き。収集時に価格が無かったレコードの再請求（既存履歴を再計算しない）。
- **体験** —— ライトウェイトモード（エッジ格納 + ホバーで今日の使用量をちらっと確認）、カスタムタイトルバー、ライト / ダークテーマ、ローカルファーストでデフォルトプライベート。
- **パッケージング** —— Windows、macOS（Apple Silicon）、Linux 向けクロスプラットフォームインストーラ。タグ push で GitHub Actions が自動ビルド。

### 既知の制限

- **macOS**: Apple Silicon（arm64）のみ。ビルドは未署名——初回起動時に右クリック → **開く**（または `xattr -dr com.apple.quarantine /Applications/cc one.app`）。Intel Mac ユーザーはソースからビルド可能。
- **プロバイダ**: 現在は Claude Code のみ。追加プロバイダ（Codex、Cursor など）を計画中。

[Unreleased]: https://github.com/Buktal/cc-one/compare/v1.7.1...HEAD
[1.6.0]: https://github.com/Buktal/cc-one/releases/tag/v1.6.0
[1.5.1]: https://github.com/Buktal/cc-one/releases/tag/v1.5.1
[1.5.0]: https://github.com/Buktal/cc-one/releases/tag/v1.5.0
[1.4.0]: https://github.com/Buktal/cc-one/releases/tag/v1.4.0
[1.3.1]: https://github.com/Buktal/cc-one/releases/tag/v1.3.1
[1.3.0]: https://github.com/Buktal/cc-one/releases/tag/v1.3.0
[1.2.0]: https://github.com/Buktal/cc-one/releases/tag/v1.2.0
[1.1.0]: https://github.com/Buktal/cc-one/releases/tag/v1.1.0
[1.0.0]: https://github.com/Buktal/cc-one/releases/tag/v1.0.0
