# 更新履歴

cc one の主な変更点をすべて記録します。

形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に基づき、本プロジェクトは[セマンティックバージョニング](https://semver.org/lang/ja/)に従います。

## [2.1.0] - 2026-09-01

### 追加

- **トレンドエリアを積み上げエリア群へ改修 + 日次コストカード** —— 4 バケットの折れ線を積み上げ面グラフのグループに昇格（バケット色の塗り・共有 stack・バケットごとの凡例表示切替を維持）、カードヘッダーに絶対値/構成比のトグルを追加。構成比モードは Y 軸を 0–100% に固定し、非表示バケットを分母から除外、ツールチップ行は「構成比 ・ 絶対量」を読み、合計行に当日の絶対量とコストを表示。新しい日次コストカードは一日ごとの総コストを頂点ラベル付きバーで描画（ラベル密度上限 14 本）、カードヘッダーに「定価推定」の注記を常設。両カードとも同じトレンドキャッシュを読み、追加リクエストはゼロ。(#119)
- **コントリビューションカレンダー + 「過去 1 年」プリセット** —— カレンダーヒートマップカードを追加：月曜始まりの週カレンダーグリッド、中性インク階調 5 段の色スケール（非ゼロ日の四分位で分割、どの量規模でも 5 段判別可能）、月/曜日ラベルは dayjs ロケールに従い、セルごとにネイティブ title で読み取り。スパンはグローバルフィルターウィンドウに追随（広い窓は横スクロール）、セルのクリック = ウィンドウをその日へ狭める。「過去 1 年」プリセットは from = 今日−364 日を動的に計算して日付を凍結せず、3 ページの日付プリセットに同時適用。(#119)
- **KPI スパークライン + 半円構成図** —— リクエスト・ヒット率・コスト・Token の 4 セルそれぞれに末尾スパークラインを追加（手描き SVG + アクセント終点、グローバルフィルターに応じて日次/時間別。ヒット率の式は `lib/token-buckets` に単一ソース化し sessions ページと共用）；セッションターン 4 段とリクエスト時間 4 段をプログレスバーから 180° 半円構成図へ昇格（弧セグメント接続 + 中心の合計 + 右側の精確な読み取り行）。(#119)
- **ディメンションカードのフィールド補完 + 最も高価なセッション Top-N** —— セッション Top-N カードに tokens/cost 指標スイッチを追加（cost は「最も高価なセッション」、並び順は ccusage のセッションレポートに準拠）、Top 行はコスト/リクエスト数/4 バケット内訳を携行。モデル行 sub にリクエスト数、プロジェクト行 sub にヒット率（キャッシュ活動なしでは非表示）、デバイス行 sub にコストを補完。(#119)

### 変更

- **コスト計算の注記を常設** —— すべてのコスト数値の横に「定価推定」の注記を常設（ccusage calculate / Claude Code のローカル推定と同系）：ヒーロー最下段、プロジェクト/デバイス/セッション行内のコストセグメント、KPI コストセル、トレンドツールチップのコスト行に一并表記し、KPI 総コストラベルも更新。(#119)

## [2.0.3] - 2026-08-28

### 追加

- **同梱バイナリ名を「CC One」に固定** —— tauri.conf.json に `mainBinaryName` を明示し、バンドルされる実行ファイルが cargo の出力名 `cc-one` ではなくアプリ名と一致する「CC One」として出荷されます（Windows ではインストール後に `CC One.exe`）。

### 修正

- **Gemini スニペットのプレビューが空約束をしない** —— 空値の env キー（例 `KEY=`）は共有可能スニペットの取り込み候補として一覧表示される一方、抽出では提供できませんでした（バリデーターが空値を拒否）。「どのキーを共有可能か」は単一の決定名簿に集約され、プレビュー候補と抽出の両方がそこから派生するため、Gemini の空値キーは候補に現れなくなりました。

### 変更

- **`imported` は行ではなく件を数える** —— 同期アラインレポート（起動ログと設定ページのトースト）の imported が、各ドメインが実際に取り込んだ件数の合計（usage の新規行・取り込んだピアセッションスナップショット数・書き込まれたプロバイダーエントリ数・読み込まれたデバイス登録行数）を示します（en/zh は行→件に文言を更新、ja は元々中立的な「件」）。
- **内部リファクタリング（動作は変わらず）** —— 14 件のアーキテクチャ候補がすべて着地、不変量を釘付ける新規テスト 40 例はすべて本番パス：
  - **同期** —— デバイスごとの JSON 同期ドキュメント（フォールトトレラント読み込み・バイト安定書き込み・latest-wins マージ）を synced_doc 単一モジュールに集約、providers / groups / devices の 3 ドメインが収編されバイト単位のゴールデンテストで担保；同期ドメイン一覧をデータ化し、静的 DOMAINS テーブルが pull/push を駆動（ドメイン追加 = 1 行追加）。
  - **プロバイダー** —— 共有可能キーの決定を単一ソースに（プレビュー候補と抽出が同じ名簿から派生）；制御軸の 3 値マージ（あり → 置換 / なし → 撤去 / リスト外 → スキップ）を共有プリミティブに集約し、Gemini の「settings.json トップレベル全体が制御対象」という裁定を明示（マージ動作はバイト単位で同一）；プロバイダー行 UPSERT の時計差異を ProviderUpsertPolicy として型付け（ローカル時計 / 作者タイムスタンプの直通）。
  - **データベース** —— 次元集計の 3 横断面（駆動投影・複合キー述語・バケットデコード）を aggregate_sql に集約；usage_records の列リスト・bind・decode を一箇所に同居させコンパイル時の長さ自己チェックを追加；「どのセッション列が git push に乗るか」を PushTrack として明示（お気に入りトラックは push され、デバイスローカル列はスナップショットに入らない）。
  - **ソース解析** —— JSONL の行単位 fold を共有 walker に集約（1 ベース番号・空行・skipped 記帳の単一ソース）、2 つのカーソル意味論（claude/codex と grok）を 2 ポリシーとして明示。
  - **フロントエンド** —— transcript 派生クラスタを独立モジュールへ移築；トークンバケット表示名簿（色順・キー順）の 4 箇所の手書きを BUCKET_DISPLAY 単一ソースに集約；5 軸フィルターバー（期間・ソース・モデル・プロジェクト・デバイス）を共有 FilterBar とし両ツールバーがバインド、ブラウザ hook の日付 6 キーを削除；ページ端ステップの判定を純関数へ精製、リネーム hook はセッション詳細へ移築。
  - **雑多** —— 変更後の push を library ドメイン入口へ沈める（呼び出し側が「ローカル変更だけで push しない」ことは構造的に不可能に）；track/column のマジック文字列を列挙型化（bindings 再生成）；「書いたら必ず sessions_changed を発火」を 2 つのコンビネータで構造的に固定；forget_device の is_self ガードをドメイン入口へ沈める。

## [2.0.2] - 2026-08-18

### 修正

- **同期 pull でローカルの資格情報が失われない** —— ピアの Codex / OpenCode プロバイダーをインポートすると、API キーがレッドアクション済みコピーで黙って上書きされていました（push は 4 箇所のキーを剥がし、pull は 2 箇所しか戻さなかった）。「キーの所在地」は成対の strip/restore を持つ単一モジュールに集約され、codex auth と opencode options も復元されるようになりました。往復プロパティテストで担保。
- **ダーティフラグが同期を詰まらせない** —— 失敗・クラッシュで残ったダーティフラグは「pushed」ゲートで再クリアされず、端末が永遠に push 1 回分遅れる状態でした。クリアを単一トランザクション化しゲートを撤去（旧コードで失敗する新テストで証明）。
- **同期途中の半端なクリア状態を作らない** —— 2 連続クリアの 1 つ目が成功・2 つ目が失敗すると「日付はクリア済み・セッションは未クリア」の中間状態が残りました。単一トランザクションで構造的に不可能になり、SQLITE_BUSY 注入テストで「全クリアか全ダーティのどちらか」を保証。
- **全キーのテンプレート変数が同期から消えない** —— 全テンプレート値がキーだと剥離がレコードごと除去し、往復でプロバイダーが消失（Bedrock AK/SK のシナリオ）。restore がレコードを再作成します。
- **Claude の保存で未展開プレースホルダーが戻らない** —— 未展開の Bedrock プレースホルダーエンドポイントがスナップショットに書き戻され、バックエンド検証に拒否されていました。フォームのミラー状態撤去後、保存は物化済みテキストから読みます。
- **OpenCode の編集が中途半端な JSON を飲み込まない** —— フィールド書き戻しが他の 4 アプリと同じガード付き書き込みになり、JSON が壊れている場合は拒否されます。

### 変更

- **内部リファクタリング（動作は変わらず）** —— 12 件のアーキテクチャ候補がすべて着地：キー所在地モジュール、per-app live adapter seam（8 分岐点を集約）、同期 5 ドメインのペア化、store 層の衝突計画 seam へのインポート収束、discovery 走査スケルトンと gate モードの明示、collect プロトコルの corrections チャネル、FilterSelect センチネルドロップダウン、paged-browser コントローラー、プロバイダーフォームのミラー状態撤去（1033→640 行）、api.ts の 7 ドメイン分割とキャッシュキー次元レジストリ、ターンナビ検索ステートマシン reducer、雑多 4 件（shell 状態クラスタ・日付範囲フィルター・エラー原文・削除確認）。新規テスト 50 以上、すべて本番パス。
- **リクエストログの空状態文言が同期状態に追従** —— マルチデバイス同期中は CTA が「同期中…」を表示（従来の一律「採集中…」から変更）、サイドバーと意味が一致。

## [2.0.1] - 2026-08-18

### 修正

- **ログイン状態版プロバイダーへの切り替えが実際に反映** —— Codex の制御アイデンティティキー（`model` / `model_providers` / `experimental_bearer_token`）は「新プロバイダーが勝つ」セマンティクス（ADR-0010）に従い、対象が持たないキーを live から撤去します。サードパーティ → 公式（ChatGPT ログイン）切り替え後、Codex CLI が旧ベンダーに黙ってルーティングされ続けることがなくなりました。Gemini の最上位 `model` キーも同じ規則です。
- **Gemini の書き込みで中途半端な状態を作らない** —— マージと検証をすべての書き込み前に完了；`settings.json` のステップが失敗したら書き込み済みの `.env` をロールバックし、両ファイルとも対称な `.bak` バックアップを取ります。
- **同じプロバイダーへの再切り替えは全アプリで無操作** —— Claude と Gemini に、Codex / Grok / OpenCode が既に持っていた「内容無変化 → 無操作」チェックを追加：ファイルの再書き込みなし、`.bak` の更新なし、mtime に触れない。書き込みトランザクション（無変化判定・バックアップ・アトミック書き込み・副ファイルのロールバック）は 5 アプリ共通の単一実装に集約されました。
- **必須キー確認が全アプリをカバー** —— 切り替え前チェックは従来 Claude の env キーしか読まず、他アプリでは発動しませんでした。アプリごとにキーセットを導出（Codex auth + TOML base_url、Gemini env、Grok TOML、OpenCode options）し、不完全なプロバイダーへの切り替えは先に確認を求めます。
- **共通スニペットの強化** —— 取得パスも保存と同じ検証を通る；Claude / Gemini のスニペットは書き込み層でも資格情報キーを拒否；Gemini のフラットキー（例 `{"GEMINI_MODEL":"m"}`）は黙って無効になる代わりに理由付きで拒否；スニペット取得時に enabled=true を強制しなくなりました。
- **セッションページングの limit をクランプ** —— 利用クエリと同じく limit を 1–1000 にクランプ（0 や巨大な limit でテーブル全体を一括ロードしなくなりました）。
- **OpenCode のインポートとプレビューで表示名の導出が一致** —— 空 `name` は両パスで provider key にフォールバック。
- **保存が実際の app を返す** —— `save_provider` の戻り値に Claude をハードコードしていた内部 shim を削除；保存した Codex プロバイダーは Codex として読み戻されます。
- **ローカライズ** —— 日本語の欠落 17 キーを復帰（英語フォールバック解消）、「有効」バッジの文言が三言語で一致、Codex / Grok のスニペット定常ヒントを平易な説明に（内部キー名は検証エラーにのみ出現）、Grok ヒントにカスタムモデルプロファイルがスニペットと共に書き込まれる旨を追記。
- **UI 修正** —— トランスクリプト読み込み失敗時に再試行、ライブラリスキャン失敗が「空フォルダー」を装わない、ライブラリ / プロバイダーのローディング状態が中央スケルトン＋エラー時再試行、プロバイダー行とセッション詳細のコントロール高を揃える、ターンナビの全文ツールチップがクリップされない、最小幅ウィンドウで列ヘッダーが溢れない、スニペットエディターがビューポート残高を埋める。
- **Codex 公式プリセットを自己説明的に改名** —— 「OpenAI (ChatGPT ログイン)」と「OpenAI (API Key)」、注記を読まなくても判別できます。

### 変更

- **内部リファクタリング（動作は変わらず）** —— 2,084 行の commands 巨石を 13 のドメインモジュールに分割；フロントエンドの重複ロジック（ソースタグ・デバイスラベル・コピーボタン・状態アクション）とパーサー / 書き込み層ヘルパーを単一の所有者に集約；README のプリセット数を 5 プール合計（59 個）に修正。

## [2.0.0] - 2026-08-14

### 追加

- **Apps & Providers がベータを卒業** —— ナビゲーションのベータフラグが外れ、各 AI CLI が本物のアプリディメンションを手に入れました：アプリ別のプリセット、アプリ別のフォームフィールド（Claude Code / Codex / Gemini CLI / Grok CLI / OpenCode）、そして各 CLI の実際の設定ファイルへのライブ書き込み。プロバイダー切り替えは、Codex の `config.toml` + `auth.json`（制御フィールドのみの TOML マージ）、Gemini CLI の `settings.json` マージ + OAuth マーカー付きの全量 `env` 更新、Grok の `config.toml` ライブマージ、OpenCode の `opencode.json` 追加モード（機密情報はレッドアクション）を書き込みます。各アプリのプロバイダーリストは端末単位でバイト単位の安定同期——API キーが端末の外へ出ることはありません。
- **CC-Switch からのインポート** —— プロバイダーリストを CC-Switch のエクスポートから直接インポート可能に。統一インポートダイアログは、ローカル設定ファイル・CC-Switch ファイル・CC One バックアップを対等なソースとして扱います（ADR-0011/0012）。ライブインポートは全アプリに一般化され、opencode.json インポートは適用前にプレビューを表示します。
- **Gemini のネイティブモデル取得** —— ベンダーの `/v1beta/models` エンドポイントが OpenAI 互換フェッチに加わり、ワンクリックで 5 つのモデルロールをすべて埋められます。
- **Codex・Gemini・Grok 共通設定スニペット** —— プロバイダー非依存のスニペット（デフォルトで attribution を非表示）は、切り替えのたびに制御フィールドのみへマージ。スイッチを反転した瞬間に有効になり、保存を待つ必要はありません。
- **セッションのトランスクリプトがマークダウンで描画** —— コードブロックと JSON がシンタックスハイライト付きでテーマに追従。
- **セッション内検索** —— トランスクリプト全体のヒットハイライトとメッセージへのジャンプ。側面には番号付きターンパネル、着地地点はリングフラッシュで通知。
- **Subagent セッション** —— Claude Code のサブエージェント実行が独立した行として表示され、エージェントタイプのバッジが付きます。
- **token 優先のモデル分布** —— 使用量チャートがトークン順でモデルを並べ、モデル別キャッシュヒット率を追加。
- **リクエストログの展開行** —— 行をクリックすると完全な詳細（トークン/コスト内訳、サービス階層）を展開。4 つのテーブルすべてが 20 行/ページの共有ページネーションバーを使います。
- **shadcn カレンダー日付選択** —— ネイティブの date input を本格的なカレンダーに置き換え。
- **プロバイダー行の磨き込み** —— カテゴリの意味色、プロバイダーの複製、作成時のデフォルト 1M、「すべてに適用」が各ロールでチェック済みの 1M を消し去らない。
- **テーマ体系** —— ダークモードに 3 段階のサーフェス階層（ページ/モーダル/入力欄）、ツールチップ専用サーフェス変数、グランスカードのブランドグラデーション。
- **ブランド刷新：VaultOne → cc one（归一）** —— アプリ・リポジトリ・パッケージが `cc-one` に統一。旧構成ディレクトリは初回起動時に自動移行されます。

### 変更

- **サイドバー再構成** —— ナビゲーショングループ（観察/管理）、滑らかな折りたたみアニメーション、フッターの軽量化、選択中グループの影響を受けないカウント行。
- **期間フィルター再構築** —— 日付跨ぎパッチを削除し、クエリはフィルターからリアルタイムに派生（ADR-0008）。
- **セッション詳細パネル** —— ヘッダーをアイデンティティ/統計に分離、前後セッションへのナビゲーション、一括折りたたみ、行選択、タブ別空状態、2 段ツールバー。
- **モーダル操作** —— 削除確認の成功パス磨き込み、エクスポートオプションのデシジョンカード化、価格エディターのマトリックス化。
- **狭いウィンドウのレイアウト** —— フィルター行はコンテナクエリで折り返し、ページネーションバーは 1 行を維持。
- **ツールチップと文言** —— 30 箇所のネイティブ `title` をアクセシブルな名前付きテーマ対応ツールチップへ。グランスモードの文言を 3 言語で統一。

### 修正

- **モーダル閉じる際のちらつき** —— ポップアップ上の単独 duration-* が Tailwind v4 で暗黙の transition を生成してアニメーションの時間トークンを汚染し、閉じる際に 1 フレーム点滅していました。ポップアップは自身の duration を持ちません。
- **Codex の「unknown model」遅延** —— モデルコンテキストがトークンイベントより遅れなくなりました。
- **ターンジャンプのハイライトずれ** —— Virtuoso の長距離ジャンプでハイライトが 1 行前へ戻ることがなくなりました。純粋な reducer（turn-nav）がターゲットを固定します（回帰テスト付き）。
- **収集後のダッシュボード一括更新** —— 集約ストアのタグが 1 箇所で無効化されます（ADR-0009）。
- **リネームで列幅が変わらない** —— `table-fixed` が名前列を安定に保ちます。
- **削除確認の busy 未解放** —— プロパティ駆動のクローズでボタンがローディングのままにならなくなりました。
- **狭いウィンドウでの重なり** —— セッション表はタイトル列を潰さず横スクロールします。

### 内部

- **ADR-0008 / 0009 / 0011 / 0012** —— 期間派生、ストアタグ無効化、インポート入口の独立、ライブインポートの一般化。
- **共有 TOML パース/クリーニング** —— codex/grok のライブ書き込みの重複が公共関数に一本化。
- **テスト付きの純粋関数** —— turn-nav reducer、トランスクリプトのハイライト抽出（唯一の真実の源）、インポートのテキスト抽出（`extractTitle` / `pairModelNameKeys`）がすべて `derive` にテスト付きで存在。

## [1.8.0] - 2026-08-07

### 追加

- **プロバイダー管理：リスト + CRUD** —— 新しい「プロバイダー」トップレベルビュー（供应商）が、保存済みの全プロバイダー（名前/カテゴリ/エンドポイント/モデル）をドラッグ並べ替え付きで一覧表示し、まもなく登場するプリセットピッカーを指す空状態も備えます。サイドパネルシート（名前/エンドポイント/API キー）でカスタムプロバイダーの作成・編集・削除が可能。API キーとエンドポイントはプロバイダーの `settingsConfig` スナップショット内に格納され、フォームはフィールド単位でそのまま保持します。データは今のところローカル SQLite の `provider` テーブルに保存——切り替え（ライブ書き込み）、プリセット、同期、モデルマッピング、JSON 編集は今後のチケットで実装予定。
- **プロバイダー切り替えがライブ設定に書き込む** —— プロバイダービューに「現在使用中」カード（アクティブなプロバイダー + エンドポイント + モデル）と行ごとの「切り替え」ボタンが追加。切り替えはプロバイダーのスナップショットを `~/.claude/settings.json` の*制御フィールドのみ*（`env` + `includeCoAuthoredBy`/`attribution`/`effortLevel`/`enabledPlugins`/`skipWebFetchPreflight`）へマージし、他のすべて——hooks、MCP サーバー、権限、プラグイン、モデル——はディスク上でそのまま残します。書き込み前のライブファイルは `settings.json.bak` にバックアップ（単一ファイル上書き）され、アトミックな一時ファイルリネームで置き換えます。アクティブなプロバイダーはローカルの `config.json` に記憶され、再起動後も維持されます。
- **18 のビルトインプロバイダープリセット** —— プロバイダービューにプリセットピッカー（検索可能、カテゴリ別グループ）が同梱され、18 テンプレートを内蔵：Claude 公式 + AWS Bedrock ×2、国内ベンダー 11 社（Kimi、DeepSeek、GLM、Volcengine、DouBao、Baidu、Alibaba、StepFun、MiniMax、MiMo など）、人気アグリゲーター 4 社（SiliconFlow、OpenRouter、ModelScope、Novita）。選択すると新規プロバイダーフォームへエンドポイント/認証/モデルマッピングが事前入力され、プリセット自体は決して変更されないため、編集してカスタムプロバイダーとして保存できます。プリセットはアプリに同梱され、同期されることはありません。
- **プロバイダーフォーム内の生 settings.json エディター** —— プロバイダーシートの末尾は、完全なスナップショットを扱う CodeMirror 6 JSON エディター：シンタックスハイライト、パースエラーまたはオブジェクト以外のトップレベルは赤い下線、フォーマットボタン（2 スペース）、ダーク/ライトテーマ追従。JSON が唯一の真実の源——編集するとフォームのエンドポイント/API キーが再導出され、フィールド編集はマージし直され、不正なスナップショットは保存をブロックして編集を静かに破棄しません。
- **プロバイダー構造が端末間で同期** —— プロバイダーリストは既存の push/pull オーケストレーションに乗り、`repo/data/<deviceId>/` 配下の端末別 `providers.json` として保存：push のたびにこの端末のファイルをローカル DB から実体化（バイト安定——ストアが変わらなければ push なし）、pull のたびに全ピアのファイルを id でマージし、`updated_at` が最新のものが勝ちます。API キーは同期ファイルに一切入りません——4 つのシークレット env（`ANTHROPIC_AUTH_TOKEN`/`ANTHROPIC_API_KEY`/AWS キーペア）は書き込み前に除去され、各端末がローカルで自分のキーを補完。ピアのキー除去コピーをインポートする際はローカル行のキーをマージし直すため、キーなしコピーが入力済みの行を上書きすることは絶対にありません。アクティブなプロバイダーはローカルの `config.json` に留まり、アクティベーションは同期されません。
- **モデルロールマッピング + 1M マーカー** —— プロバイダーフォームに 5 ロールのモデルマップ（Sonnet / Opus / Fable / Haiku / Subagent）が追加され、各ロールに表示名・リクエストモデル・1M コンテキストチェックボックス（Haiku は自動的にマーカーを除去——1M 非対応）。プロバイダー設定にないロールは読み込み時にバックフィルされ、「すべてに適用」アクションは入力済みモデルをワンクリックで全ロールへ展開します。
- **モデルリスト取得** —— モデルマップセクションの「モデル取得」ボタンは、バックエンドからベンダーの OpenAI 互換 `/v1/models` エンドポイントを叩きます（webview の fetch は CORS に当たるため）：明示的な `modelsUrl` 上書きが優先され、バージョンセグメントで終わるベース URL は 2 つ目の `/v1` ではなく `/models` を追加、既知の Anthropic 互換サブパスは除去され、候補は重複排除して最大 3 つ順に試行。返されたモデル ID はワンクリックで 5 ロールすべてに反映。失敗はバケット化トーストで通知（認証失敗/エンドポイント切断/タイムアウト/形式非対応）。
- **共通設定スニペット** —— プロバイダー非依存の設定スニペット（デフォルト `{"includeCoAuthoredBy": false}` で attribution を非表示）はプロバイダーフォームに置かれ、手書きして有効化すると、切り替えのたびに制御フィールドのみへマージ：`env` はキー単位で深くマージされ（プロバイダー自身の値が優先）、他の制御スイッチはプロバイダーが未設定のときだけ埋められ、非制御フィールドは一切触れません。
- **認証フィールド切り替え + テンプレート変数** —— フォームの認証フィールドは `ANTHROPIC_AUTH_TOKEN` と `ANTHROPIC_API_KEY` を切り替え、切り替え時に入力値を移動します。Bedrock プリセットはさらにテンプレート変数（region / access key / secret key）を表示し、保存前に `${VAR}` プレースホルダーへ再帰的に代入。未入力の変数は保存をブロックし、壊れた設定を書き込みません。
- **プロバイダーのエクスポート/インポート** —— プロバイダーリスト全体を 1 つの JSON 文書としてエクスポート（オプションで API キー含む）し、任意の端末にインポート：マージモードは既存行に触れず id で重複排除、上書きモードはインポートコピーを優先。純粋に手動の移行パス——インポートはローカル DB のみへ行き、git 同期は経由しません。
- **ユーザーターン間のジャンプ** —— セッションを開くと、トランスクリプトの横にすべてのユーザーメッセージを並べるスリムなパネルがマウントされます（約 16 文字幅、はみ出しは省略記号）。行をクリックしてそのメッセージへ直接ジャンプ、読んでいるメッセージの行はハイライト維持、任意の行にホバーで全文表示。2 つのパネルは同時に開閉します。
- **ジャンプがターゲットを点滅** —— ナビパネルでターン番号をクリックすると、着地したメッセージがアクセントリングで 3 回パルスし、目がスクロールと同じ場所へ正確に到着します。

### 変更

- **ソースパーサーを `SourceParser` に改名** —— ログパーサーモジュール（`providers/`）は `source_parser/` になり、"Provider" という言葉を今後のベンダー管理機能に譲ります。純粋な改名、動作の変更なし。
- **リクエストログテーブルから重複 Source 列を削除** —— 同じ `source` フィールドから導出された 2 列（古い "(Session)" サフィックス付きの表示名と生タグ）が 1 列に統合：表示名 + ホバーで生タグ。誤解を招くサフィックスは消えました——各行はセッションではなく単一の API 呼び出しです。
- **長いセッションが即座に開く** —— トランスクリプトは仮想化リスト（react-virtuoso）で描画され、ビューポート近くのメッセージだけが DOM に保持されます。数千メッセージのセッションでも全行を一度にレイアウトせず、長さに関係なくスムーズにスクロールします。
- **メッセージヘッダー行がきれいにミラー** —— アシスタントとユーザーの両ボイスで、ツールバー（折りたたみシェブロン + コピー）と時刻/モデルブロックがバブルの両端に固定され、短いユーザーメッセージが両者を端に押し込むことがなくなりました。

### 修正

- **ターンジャンプが行き過ぎない** —— ジャンプは以前、滑らかにターゲットを越えて推定位置までスクロールして戻り、トランスクリプトの下端まで走ってからメッセージへ跳ね返るのが見えていました。今は即座に着地し、リングフラッシュがフィードバックを担います。
- **840×600 の最小ウィンドウが実際に効くように** —— 軽量モード復元時に古い 720×520 の下限が `set_min_size` で再適用され、引き上げた最小サイズを静かに上書きしていました。グランスカード往復後のダッシュボードが欠け小さいサイズで復元されなくなります。
- **狭いウィンドウでセッション表が重ならなくなった** —— 固定 7 列がタイトル列を 0 まで圧縮し、ヘッダー文字がプロジェクト列に溢れていました。表は読みやすい幅を保ち、あふれた分は横スクロールします。

## [1.7.1] - 2026-08-06

### 追加

- **起動時にアップデートを確認** —— 起動のたびに GitHub Releases を確認し、起動中は 6 時間ごとに再確認します。新しいバージョンがあるとフッターに表示されます。

- **グループのドラッグ並べ替え** —— セッションサイドバーの任意のカスタムグループを掴んで新しい位置へドラッグできます。並べ替えた順序はそのまま維持され、お気に入りタブでは他のすべてのデバイスにも同期されます。順序はトラックごとに保存され（ローカルグループはデータベースの position 列、同期グループは同期ファイルの position フィールド）、新規グループは常に末尾に追加されます。「すべて / 未グループ」の行は固定されたままです。

### 変更

- **余裕のある最小ウィンドウ** —— メインウィンドウの最小サイズを 840×600 に引き上げ、セッション一覧がグループサイドバーに重ならないようにしました。狭いウィンドウではセッションカードのタイトルが省略記号で切り詰められます。

- **スッキリしたグループサイドバー** —— グループ行からフォルダアイコンを削除し、サイドバーを細くしました。セッションテーブルに余裕が生まれます。

- **セッション詳細パネルの磨き込み** —— タイトルのリネーム発動領域を「タイトル＋鉛筆アイコン」のみに縮小し（空白クリックで編集が始まらないように）、ソースをタグチップとして表示し、お気に入りボタンの高さをグループ選択と揃え、吹き出しは狭いウィンドウで 80% に上限（広いウィンドウでは従来どおり 72ch）。

## [1.7.0] - 2026-08-06

### 追加

- **すべてのメッセージにコピーボタン** —— セッションのトランスクリプトでメッセージ行にホバー（またはキーボードフォーカス）するとコピーボタンが現れ、クリックで原文をクリップボードにコピーし、一瞬チェックマークを表示します。
- **ウィンドウに追従する詳細パネル** —— セッション詳細パネルはウィンドウの約 7 割（`100vw - 32rem`）を占め、背後にサイドバーと一覧のタイトル列が見えたままになるため、どのセッションを開いているか常に分かります。

### 変更

- **3 声部のトランスクリプトレイアウト** —— assistant メッセージは左、user メッセージは右（ミラーした吹き出し、角は外側に向けて切る）、tool と system 行は中央に全幅で並ぶ「ワークベンチ」。位置だけで誰の発言かが分かります。
- **より締まったメッセージヘッダー** —— アイコン・時刻・モデルバッジが 1 行に並びます。user メッセージではグループが吹き出しの右端にミラーされ、時刻が右端に揃います。
- **折りたたみ可能なメッセージ、静かな tool 行** —— どのメッセージもクリックで折りたたみ（デフォルトは展開）。tool 行はデフォルトでツール名 1 行に折りたたまれ、出力は等幅フォントのコードパネルとして表示されます。
- **グループ件数の整列** —— グループサイドバーの件数は常に右端に揃い、「すべて / 未グループ」の通常行と一致。編集メニューはホバー時に件数をスライドさせて現れ、通常時はスペースを占めません。

### 修正

- **tool 行の整理** —— 重複していたアイコン 2 つを削除。名前のないツールは内容の先頭行を切り詰め、行を引き伸ばしません。

## [1.6.0] - 2026-08-06

### 追加

- **セッションブラウザ** —— サイドナビゲーションに新エントリが加わり、AI CLI が書き出す生のセッションログをブラウズ・検索可能な履歴にします。各セッションはプロジェクトディレクトリの下に並び、完全なトランスクリプト・セッションごとのトークン内訳・コスト（使用量レコードからのライブ集計で、二重保存は一切なし）を表示。期間 / ソース / モデル / デバイスでフィルタし、タイトルとパスで検索。その場でのリネームにも対応し、どのセッションも詳細パネルで開けます（ロール別に色分けされたトランスクリプト：assistant はモデルバッジ付き、user は別トーン、tool 行は折りたたみ可能）。
- **2 タブ、2 通りの整理方法** —— **ローカル** タブはこの端末で収集した全セッションを、決して端末の外に出ないプライベートグループに整理して表示。**お気に入り** タブは全デバイスでお気に入り登録したセッションを、すべての端末で共有される同期グループに整理して表示し、各エントリにソースデバイスが付記されます。同じセッションはタブごとに別のグループに属せます。
- **お気に入りの端末間同期** —— スターを付けると、そのセッションのトランスクリプトと同期グループへの配置が同期リポジトリ経由で全デバイスに公開されます。スターを外すとどこからでも消えます。端末の外に出るのはお気に入り登録したセッションだけ——それ以外はすべてローカルに留まります。
- **すべてのセッションのトランスクリプトが即座に閲覧可能** —— 会話本文は収集時にすべてローカルデータベースへ書き込まれるため、お気に入り登録の有無に関係なくどのセッションもすぐ完全なトランスクリプトを開けます。書き込み中のログファイルを再読取することはありません。
- **より速い収集** —— スケジューラのプリセットに 5 秒間隔が加わり、ダッシュボードがほぼリアルタイムになります。

### 変更

- **同期をダーティデイ追跡で再構築** —— collect はローカルストアだけに書き込み、同じトランザクション内で影響を受けた日をダーティとして記録。push はストアから当日分のアーティファクトを決定論的に再生成し（バイト安定、append ではない）、push が確定してからダーティを解除します。従来の JSONL-first 書き込み順、with-own-data のスナップショット/復元保護、アーティファクト欠落の reconcile はすべて撤去——書き込み経路は 1 本だけになり、2 台の端末が同一ファイルの内容について食い違うことはあり得ません。
- **セッションスナップショットは書き込みではなく派生に** —— お気に入りセッションの同期スナップショットは push 時にストアから再計算されます。ソースログが消えたセッションは全デバイスで自動回収されます（ローカル行と同期スナップショットの両方を削除）。
- **同期の対象範囲を縮小** —— 任意の同期リポジトリが運ぶのは、使用量・お気に入りセッション・ライブラリファイルのみになりました。1.5.x の Sync-config（リポジトリ経由でアプリ設定を同期する機能）は削除され、設定は再び端末ごとに閉じています。
- **より賢いプロジェクト分類** —— セッションのプロジェクトディレクトリは、そのセッションのイベントで最も多く出現する作業ディレクトリ（最初の 1 件ではなく最頻値）から導出されます。サブディレクトリで始まったセッションも本当のプロジェクトルートに分類されます。

### 修正

- **セッション収集の堅牢化** —— 文字の途中で終わるログファイル（書き込み中のセッション、例：中国語）は丸ごと破棄せず lossy 読み取りに切り替え、進行中のセッションがファイルごと失われることがなくなりました。スキャンカーソルは正常に進み、リネームに追随してタイトルが更新され、欠落したセッションは補完されます。
- **お気に入り未登録のセッションも閲覧可能に** —— 従来は開こうとするとお気に入り登録を求められました（トランスクリプトがお気に入りスナップショットの中にしかなかったため）。トランスクリプトはローカルデータベースを読むようになり、お気に入り登録の有無に関係なくどのセッションも開けます。
- **お気に入り状態のちらつきを解消** —— 再フェッチが古いスナップショットにフォールバックしなくなり、お気に入り登録直後のセッションは登録状態を維持。スターアイコンもテーブル行と一致します。
- **セッションのレイアウト** —— 「新規グループ」ボタンが常に表示され、グループサイドバーが領域からはみ出さなくなりました。

### 内部

- **これまで最大のアーキテクチャ整理** —— db の god-module をドメイン別 module（`store_*`）に分割。22 個の command をドメイン module から `commands.rs` へ移行し、モデル正規化・価格マッチング・デバイスレジストリ・provider 解析・UI フォーマット・日付範囲チップなど、単一情報源への収斂を全面的に実施。sync の god-module は git プリミティブ + flow オーケストレーションの 2 層に分割し、レビューによる整理で残っていた乖離を解消しました。ユーザーに見える変化はありません——ダッシュボードのコードはより拡張しやすくなっています。

## [1.5.1] - 2026-07-31

### 修正

- **ローカルコミットがリモートのパッチと重複する場合も同期が自己修復** —— 1.5.0 の rebase 自己修復は、ローカルコミットがすでにリモートにあるパッチと重複する場合（例：2 台の端末がそれぞれ同じデバイス整理を実行）に中断し、`rebase onto remote tip would conflict ... this patch has already been applied` と表示されて端末が再び分岐状態のまま固まることがありました。`pull` は rebase 時に適用済みのコミットを破棄して続行するようになり、分岐の自己修復が止まらなくなります。
- **使用量行が同期から静かに欠落しなくなる** —— ingest は従来 SQLite を先に書き、JSONL アーティファクトは単なるバックアップ扱いで追記エラーを飲み込んでいました。DB には入ったがアーティファクトに漏れた行（一時的な追記失敗、または ≤1.3.x の残骸）は以後永久にロックされ——台帳の重複除外が以降の collect をすべて静かに無視し、アーティファクトを pull したピアには永遠に現れませんでした（同じフィルタで一方の端末が約 24M token、ピアが約 30M と表示される例も）。ingest は JSONL アーティファクトへの追記を先に冪等に行い、追記エラーを伝播します。追記に失敗するとスキャンカーソルは動かず、次回の collect で同じソース行を再解析します。新設された collect 前の reconcile は、ストアにありアーティファクトにない行があるとカーソルをクリアし、1 回の再スキャンで既存の欠落をバックフィルします——端末は手動修復なしで収束します。

## [1.5.0] - 2026-07-31

### 追加

- **Grok CLI** —— Grok Build のセッションログからトークン使用量を読み取る、5 つ目の対応 AI CLI（本バージョンのリリースノートで記載漏れとなっていたため、遡って追記）。

### 変更

- **設定のレイアウト** —— 独立した Cloud-config セクションが Sync に統合されました。"Sync config" ボタンと競合リゾルバは "Sync now" と並び、1 枚の Sync カードにまとまります。セクション順は General / This machine / Devices / Sync / Maintenance になり、"Sync cloud config" ボタンは "Sync config" と表示されます。

### 修正

- **分岐した push の後の同期自己修復** —— デバイスが push 競争に負けた場合（別のピアが pull と push の間に先に push）、以後すべての "Sync now" / "Sync config" が `pull would diverge on 'main'; refusing to auto-merge` で失敗し、自分では回復できずダッシュボードは古いデータのままでした。`pull` はデバイス固有のローカルコミットをリモートの先端に rebase してから push し、分岐を自動修復します。デバイス分離（`data/<deviceId>/`）により rebase は無衝突で、両端末のデータはリモートに並存します——ソフトリセットのみの修復ではローカルツリーをそのまま再生してピアのデータを破壊していたところでした。
- **過去 1 日を選んだときのトレンドチャート** —— 過去の 1 日（例：2026-07-30 → 2026-07-30）を選ぶと使用量トレンドが平らなゼロ線に潰れていました：チャートは選択した日ではなく *今日* の時間帯をゼロ埋めしていたため、実際のレコードが一致しなかったのです。現在は選択した日の 24 時間軸（00:00 → 23:00、当日は現在時刻まで）を埋めるようになりました。

## [1.4.0] - 2026-07-30

### 追加

- **Library —— 端末別ファイル中継** —— ファイル / ディレクトリをウィンドウにドラッグするとアップロード（＝同期リポジトリの該当端末サブツリーへの push）。ディレクトリを段階的に掘り下げ（アップロード・エクスポート・単一ファイルダウンロードは任意の深さで動作）、アプリ内でファイルをプレビュー（画像は幅にフィット、ctrl+ホイールでズーム、それ以外はサンドボックス iframe）、任意のパスへエクスポート。アップロードが唯一の自動方向で、エクスポートは手動のまま、AI ツール自身の設定ディレクトリには一切書き込みません。同名同種は上書き（git 履歴が安全網）、同名異種は拒否。ピアの忘却ではそのファイルを `from-<peer>/` 配下へ移行するか削除するかを選べます。

### 変更

- **ピッカーのラベル** —— ログとダッシュボードのデバイス / ソース / モデルのドロップダウンが薄い `.` 付きプレースホルダを廃止。「すべて」は正式ラベル（すべてのデバイス / すべてのソース / すべてのモデル）を表示し、日付範囲チップは同日内の範囲を折りたたみ、ダッシュボードの狭い列で折り返さなくなりました。

### 修正

- **使用量フィルタ** —— ダッシュボードフィルタが具体日付ではなく期間プリセット（today / 7d / 30d / all / custom）を保持するようになり、「今日」が午前0時以降に「昨日」として読み戻されることがなくなりました。プリセットの無い従来レコードは "custom" にフォールバックしリテラル日付を維持します。
- **同期の重複除外** —— `usage_records` / `turn_durations` / `ledger` が `uuid` をグローバル主キーとしていたため、同一ソースイベントが2つのデバイス id で再再生されると1行に畳まれ、あるデバイスのデータが別デバイスに帰属する恐れがありました。重複除外を `(uuid, device_id)` で行うよう変更（既存の単列主キーを複合キーへ移行、行数は保持）。同期リポジトリのバインド直後に pull し、ピアデバイスが再起動なしで出現するように。
- **ウィンドウ最小サイズ** —— 変形するメインウィンドウがグランスカードの小さなサイズで復元・スナップショットされることがありました。完全ダッシュボードは 720×520 の最小サイズを強制（ライトウェイト dock はクリア、完全復元で再適用）し、古い最小サイズ未満の rect は次回復元時に自己修復します。

## [1.3.1] - 2026-07-28

### 変更

- **ソースフィルタの可視性** —— ソースデータが1つでも存在すればソースドロップダウンをレンダリングし、単一ソースのユーザーにもフィルタが見えるように（以前は収集ソース ≥2 が必要でした）。
- **フィルタチップのサイズ** —— ログコントロールバーのフィルタチップを典型的な内容でサイズ調整（モデル `w-48`、ソース `w-40`、デバイス `w-36`）。ダッシュボードのカード列は引き続き均一幅 `w-36`。

### 修正

- **リリースの完全性** —— `v1.3.0` が2コミット早くタグ付けされており、上記のソースフィルタ・チップサイズ変更が 1.3.0 インストーラに含まれていませんでした。`v1.3.1` は現在の `main` にタグ付けしてそれらを取り込みます。

## [1.3.0] - 2026-07-28

### 追加

- **マルチ CLI の使用量収集** —— 使用量収集が、ローカルにログを書く 4 つの AI CLI に対応しました：Claude Code（`~/.claude`）、Codex（`~/.codex`）、Gemini CLI（`~/.gemini`）、OpenCode（SQLite）。各ソースのトークン口径は同じ 4 バケットモデルに正規化されます——Codex のキャッシュ込み input は fresh input に変換、Gemini の `thoughts` は output に折込、OpenCode の `cache.write` は cache creation に対応付け。Claude Code の重複除外は最適な message-id スナップショット（`stop_reason` があるもの、なければ最大 output）を選ぶようになり、message_start スナップショットが固定されて output を過小計上することはなくなりました。gpt-5.5 / 5.4 / 5.2（プレフィックスフォールバックで `-codex` 系をカバー）と DeepSeek v3.x のシード価格を追加。
- **デバイススコープの使用量追跡** —— ダッシュボード、展開済みライトウェイトカード、格納ミニバーの 3 ウィンドウすべてをデバイス単位で絞り込めます。統一されたデバイスピッカーが 3 ウィンドウを駆動。格納バーのホバードロワーはホバーで開き、クリックでデバイス一覧を表示。
- **デバイスライフサイクル** —— ピアデバイスをローカルから忘却（行とアーティファクトディレクトリを削除）。git 上に存在しなくなった古いピアは、同期 pull と収集パスの両方で約 30 秒以内に自動消去され、まだアクティブなピアは次回同期で自己修復します。最近のリクエストとログのデバイス列にデバイス名が表示されるように。
- **使用量フィルタの永続化** —— 期間 / モデル / デバイススコープのフィルタがアプリ再起動後も保持されます。
- **形状別ウィンドウ座標の記憶** —— full / expanded / tucked の各形状が自身の配置と状態を記憶し、形状切替時にリセットされず前回位置が復元されます。
- **システムプロキシ追従の同期** —— libgit2 の push/fetch/clone/connect が OS のシステムプロキシ（環境変数、次に Windows レジストリ）に追従するようになり、Clash/Mihomo や企業ゲートウェイ配下の同期クライアントが黙ってタイムアウトすることはなくなりました。プロキシ変更は次回同期で即時反映、キャッシュしません。

### 変更

- **UI の洗練** —— タイトルバーを 36px に引き上げ、画面中央のデバイスセレクタを引き締めました。トークン列のヘッダには言語中立な `tok` 単位を採用。セレクトポップオーバーは内容に自動フィット（トリガより狭くならない）し、既定で上端揃えで開き、モデルドロップダウンの飛びを修正しました。

### 修正

- **Linux トレイ** —— 「Show」エントリを追加し、トレイからウィンドウを復元できるように。libappindicator/SNI バックエンドは Linux でトレイクリックイベントを一切発火しないため、左クリックが無効でした。Windows/macOS の左クリック復元は変更ありません。
- **シード価格** —— 不正なシードリテラルは起動時に panic するようになり、黙って 0 を返してすべてのコスト計算を歪めることがなくなりました。
- **本日のトレンド** —— 「本日」のトレンドが現在時刻で止まらず、1 日全体をカバーするように。
- **リクエストログ** —— フィルタ切替時にログページをリセットし、古く範囲外のページに迷い込まないように。

## [1.2.0] - 2026-07-24

### 追加

- **ライトウェイトグランスモード** —— メインウィンドウが画面右端にドッキングした小さな常時最前面の「今日」スナップショットに変形します。互いに切り替え可能な 2 つの形状：今日のトークン合計を常時表示する格納ミニバーと、ダッシュボードのアンカーを再利用した展開カード。full ⇄ expanded ⇄ tucked をどの形からも切り替え。
- **マルチスキンテーマ** —— 5 つのアクセント＋チャート配色（Neutral / Sage / Azure / Crimson / Mauve）でアプリ全体をリカラー。既定は Neutral（グレースケール）。デバイスごと、同期対象外。

### 変更

- **使用量トレンド** —— トレンドチャートが単線からデータポイント付きマルチラインになり、各指標が独自に読み取れるように。

### 修正

- **ライトウェイトモード** —— 格納バー全体がドラッグ可能に（小さな隅のグリップだけではなく）。押下で「クリックで展開」と「ドラッグで移動」を引き続き区別。

## [1.1.0] - 2026-07-23

### 追加

- **自動更新** —— 起動時に新バージョンを確認（24 時間に 1 回までスロットル）、または設定から手動確認。署名付きインストーラーを GitHub Releases から直接ダウンロード・インストールし、Ed25519 署名検証とワンクリック再起動に対応。GitHub 経由で完全に配信——セルフホストサーバー不要。更新失敗時は Releases ページを開く手動フォールバック。
- **表示言語** —— UI を English / 简体中文 / 日本語 で切り替え。

### 修正

- **ライトウェイトモード** —— 格納された覗き見アイコンのエッジ吸着と、斜め方向の展開アニメーションを滑らかに。

## [1.0.0] - 2026-07-23

初の公開リリース（オープンソース）。

### 追加

- **ダッシュボード** —— 4 バケットのトークン消費（input / output / cache creation / cache read）、キャッシュヒット率（`cache_read / (input + cache_creation + cache_read)`）、総リクエスト数と総コスト（USD、収集時に固定）、トークン対コストの双軸使用量トレンド、コールごとのリクエストログ（モデル・トークン内訳・コスト・ターン所要時間・`stop_reason` / `service_tier` チップ）、ターンごとのコストと実所要時間ビュー。
- **収集** —— Claude Code セッションログの読み取り専用解析（ソースログは変更しない）、カーソルベースの増分スキャン、トレイ常駐のバックグラウンドスケジューラ。プラグイン可能なプロバイダアーキテクチャ（現在は Claude Code、追加予定）。
- **同期（任意）** —— スタンドアローンモード（完全なダッシュボード、ネットワークゼロ）と同期モード（自分が管理する GitHub リポジトリ経由で端末間の使用量を整合）。プレーンテキストのアーティファクトを端末と日付で分割（`data/<device>/usage-YYYY-MM-DD.jsonl`）。
- **コストと価格** —— 編集可能なモデルごとの価格上書き。収集時に価格が無かったレコードの再請求（既存履歴を再計算しない）。
- **体験** —— ライトウェイトモード（エッジ格納 + ホバーで今日の使用量をちらっと確認）、カスタムタイトルバー、ライト / ダークテーマ、ローカルファーストでデフォルトプライベート。
- **パッケージング** —— Windows、macOS（Apple Silicon）、Linux 向けクロスプラットフォームインストーラ。タグ push で GitHub Actions が自動ビルド。

### 既知の制限

- **macOS**: Apple Silicon（arm64）のみ。ビルドは未署名——初回起動時に右クリック → **開く**（または `xattr -dr com.apple.quarantine /Applications/cc one.app`）。Intel Mac ユーザーはソースからビルド可能。
- **プロバイダ**: 現在は Claude Code のみ。追加プロバイダ（Codex、Cursor など）を計画中。

[2.0.1]: https://github.com/Buktal/cc-one/releases/tag/v2.0.1
[2.0.0]: https://github.com/Buktal/cc-one/releases/tag/v2.0.0
[1.8.0]: https://github.com/Buktal/cc-one/releases/tag/v1.8.0
[1.7.1]: https://github.com/Buktal/cc-one/releases/tag/v1.7.1
[1.7.0]: https://github.com/Buktal/cc-one/releases/tag/v1.7.0
[1.6.0]: https://github.com/Buktal/cc-one/releases/tag/v1.6.0
[1.5.1]: https://github.com/Buktal/cc-one/releases/tag/v1.5.1
[1.5.0]: https://github.com/Buktal/cc-one/releases/tag/v1.5.0
[1.4.0]: https://github.com/Buktal/cc-one/releases/tag/v1.4.0
[1.3.1]: https://github.com/Buktal/cc-one/releases/tag/v1.3.1
[1.3.0]: https://github.com/Buktal/cc-one/releases/tag/v1.3.0
[1.2.0]: https://github.com/Buktal/cc-one/releases/tag/v1.2.0
[1.1.0]: https://github.com/Buktal/cc-one/releases/tag/v1.1.0
[1.0.0]: https://github.com/Buktal/cc-one/releases/tag/v1.0.0
